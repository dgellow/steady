#!/bin/bash
set -e

# Bootstrap the Steady project
# Caches all dependencies and initializes submodules

echo "Bootstrapping Steady..."

# Initialize git submodules (JSON Schema test suite)
git submodule update --init --recursive

# Cache Deno dependencies
deno cache cmd/steady.ts

echo "Bootstrap complete!"
